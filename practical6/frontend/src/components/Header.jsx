function Header({ name }) {
  return (
    <header className="header">
      <h1>{name}</h1>
      <p>Practical 6: React + Node + MongoDB Task Management App</p>
    </header>
  );
}

export default Header;
